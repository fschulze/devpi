[tool.ruff]
extend = "../pyproject.toml"
target-version = "py310"


[tool.ruff.lint.extend-per-file-ignores]
"ruff-format-diff.py" = [
    "ASYNC240", # maybe cleanup later
]


[tool.ruff.format]
exclude = [
    "cleanup_devpi_indices.py",
]
